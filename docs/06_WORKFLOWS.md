# Workflows

## Asset Registration

- Add a single asset from `/assets/new`.
- Clone an existing asset from Asset Register or Asset Detail when adding another similar asset. The clone flow opens `/assets/new?cloneFrom={assetId}`, copies shared master, ownership, location, purchase, warranty, custom-field, and linked purchase-document data, but leaves Asset Tag, Serial Number, and FA/accounting code blank for the new record.
- Add a batch of assets from the same page using shared purchase/master data plus row-level serial/manual asset tag data by default. When a batch contains assets spread across holders, departments, or sites, open the optional row columns for custodian, department, current location, home location, and remark; blank row fields fall back to the shared batch values.
- Single and batch create auto-select the model when the selected category and brand uniquely match one active model; users can still choose a specific model when more than one exists.
- Import legacy assets from Excel through the import/export tools.
- Asset tags may be manually entered for legacy assets or auto-generated when left blank.
- Use the Asset Register quick filters for common operational queues such as missing Serial, missing photo, purchase-info gaps, warranty expiring, missing responsibility, Ready, In Use, Checked Out, Pending Repair, and Under Maintenance. Quick filters are grouped by data quality, ownership/custody scope, and lifecycle status, update the URL, and preserve the current search/company/branch/category context. On mobile the groups scroll horizontally so the first asset cards are not pushed far below the fold.
- Asset Register accepts drilldowns from master data counts: category asset counts open the register with `categoryId`, brand asset counts open it with `brandId`, and model asset counts open it with `modelId`. Active Brand/Model drilldowns appear as removable chips above the register filters and stay preserved when applying additional filters.
- The Asset Register filter panel keeps the daily filters visible first: search, company, branch, and category are the primary controls. Status, condition, ownership type, and rows-per-page live in a collapsible advanced section that opens automatically when one of those filters is active. Status and condition stay grouped with compact help icons.
- The Asset Register search box can find assets by Asset Tag, name, Serial, Fixed Asset Code, master-data codes, custodian name, and the current custodian's employee code. It intentionally searches only the current holder's `Employee.code`, not repair reporters, repair assignees, or other employees related through history.
- The register Import Wizard starts collapsed and appears after the table/card list so daily list work stays first; open it only when importing Excel. The table defaults to the Operations column preset before stored browser preferences load, then remembers All, Operations, Accounting, and Audit views in that browser. Column/export/template utility buttons are desktop-only; mobile users work from the compact asset cards.
- Opening asset detail, edit, or clone from the register preserves the current register URL through a sanitized `returnTo` value. Back, cancel, and save actions return to the same page, filters, sort, and search state instead of resetting the operator to the first page.
- Company and branch on the asset form are the asset owner/tag scope; they drive generated asset tags and reporting/accounting scope.
- Single asset create/edit forms keep Save and Cancel in a fixed bottom action bar so operators do not need to scroll to the end of the long registration form before saving. The existing duplicate, protected-status, and saving guards still control whether Save is enabled.
- Custodian defaults to the selected owner company/branch/department scope, but the form can opt in to cross-company custodians for cases where the asset owner and current holder are different organizations.
- When editing an existing asset, a saved custodian outside the owner company/branch/department scope automatically opens the cross-company custodian mode so the current holder remains visible and can be reviewed before saving.
- Cross-company custodian selections show an explicit warning and are recorded in the creation audit trail. Batch create follows the same model for shared owner/tag scope plus common or row-level custodian values.
- Home/current location defaults to the selected owner branch, but single and batch create can opt in to cross-branch locations when the physical site differs from the branch used for Asset Tag ownership. Cross-branch mode warns the operator and appends each location's branch label in the dropdown to avoid ambiguous repeated location codes.
- Use the Asset Register cross-scope quick filters when reviewing assets that are held or physically located outside their owner/tag scope. The filters separate all cross-scope cases, custodian different company, custodian different branch, and current location different branch while preserving the normal search/company/branch/category context.
- The Dashboard highlights active cross-scope assets as a high-level KPI and review panel. Use those cards as the daily entry point for ownership/custody scope cleanup; they drill into the same Asset Register cross-scope filters and do not change custody, transfer, or master-edit rules.
- Clone sessions show a warning banner reminding users to review custodian, location, purchasing data, and linked documents before saving. Batch mode is hidden during a clone session so the prefilled single-asset draft is not lost by switching modes.

## Asset Scan And Labels

- Print Asset Label QR codes from the label workflows, using the configured Public QR Base URL when printing for production.
- Use `/asset-management/labels` for high-volume label preparation. The unprinted/printed/recent queue can be filtered by company, branch, category, location, and created date; the queue action summarizes whether the current batch scope is all assets, a company, a branch, or a specific location so users can print whole company/branch batches without hunting through a long location dropdown. Branch/location choices include hierarchy context so repeated branch or location names stay distinguishable. Selected labels can be sorted by selection order, Asset Tag, location, or category before printing.
- The label search box keeps its pre-search guidance as a small hint under the input. Users should type at least two characters to search ad hoc assets, while batch work should start from the queue filters and the summarized "add by current filter" action.
- Label queue titles use the asset name first and only append brand/model text when it adds new information, avoiding repeated strings when the asset name already contains the brand or model. Print templates support `{labelAssetName}` for a computed label-only name that can remove the category prefix from suggested asset names when the `Admin > Settings > Label / QR` `ชื่อทรัพย์สินบน Label` segmented control is set to `ย่ออัตโนมัติ`; `ชื่อเต็ม` prints the full master-data name. `Asset.name` and `{assetName}` remain unchanged. The Brother tape guidance on print pages keeps a dynamic tape-size placeholder so changing the print profile still updates the instruction text safely.
- Use `/asset-management/scan` for field lookup of printed Asset Labels. This page is QR-first, recognizes `/q/a/{assetId}` resolver URLs, stops the camera after a successful read, and opens the asset detail automatically. On supported mobile browsers, the camera preview also exposes flashlight and 1x/2x/3x zoom controls for small labels or low-light rooms.
- On mobile, the Asset Label scanner uses an undistorted 4:3 camera preview with a smaller square QR guidance frame. It defaults to the generic environment-facing rear camera when multiple devices are reported, requests high-resolution 4096x3072/30fps ideal constraints with continuous focus/exposure where supported, and best-effort applies the camera track's maximum reported resolution after the stream opens. Supported QR camera tracks expose bounded 1x/2x/3x zoom controls to scanner surfaces so users can hold the label slightly farther away while keeping the QR large, then return to the unzoomed 1x preview without restarting the camera; unsupported devices hide those controls. Asset QR decoding uses a shared direct `getUserMedia` stream plus ZXing `BrowserQRCodeReader.decode(video)` so it reads the native-resolution video frame rather than the CSS-pixel hidden canvas used by `html5-qrcode`; this avoids mobile native detector gaps and mirror-flip retries. Users should center only the QR code in the frame and adjust distance until the QR modules are sharp. Reusable Serial Number scanner inputs use the same native-resolution camera path with ZXing `BrowserMultiFormatReader`, optional browser `BarcodeDetector`, focused scan-band crops that decode crop `ImageData` directly through ZXing `RGBLuminanceSource`/`BinaryBitmap` before full-frame fallback, QR plus common 1D/2D barcode formats, a 16:9 preview, and a narrow barcode guide with scan line for manufacturer labels; users should center the barcode band on the line, make the bars fill most of the frame, and keep the barcode lines sharp.

## Asset Custody

- Check-out records who receives the asset and captures handover evidence.
- Check-in records return and receiving parties and can route the asset to Ready, Pending Repair, or Pending Disposal.
- Legacy/imported assets that already have a current custodian but no active checkout are handled from the Check-in page through the "ข้อมูลเก่า" panel. Search the panel by Asset Tag, asset name, current holder, or location; then create the backfilled handover before completing the normal return form. This creates `AssetCheckout`, `AssetMovement`, and `SystemLog` records with `legacy_return_backfill` so the return stays auditable instead of bypassing the check-in workflow.
- Transfer records location/custodian/department movement.
- Checkout and transfer are the normal workflows for custody changes after registration; the asset owner company/branch remains the tag, reporting, and accounting scope unless the asset master data is intentionally edited.
- Asset Detail shows a unified activity/custody timeline.
- Asset Detail keeps right-rail photos compact and keeps `รูปทรัพย์สิน` focused on register/model photos, checklist photos, and direct asset attachments. Cross-workflow evidence from purchase, components, handover/return, maintenance, audit, and disposal opens from the Evidence Center drawer as a searchable/filterable file index, while the timeline remains the main place to read the event history.
- Asset Detail relationship map shows parent/component structure as a three-lane read-only map: what the current item is installed under, the current item, and any components installed under it. The status summary states whether the current item is a parent asset, component, both, or standalone. Relationship cards prioritize the full Asset Tag, then asset name, then role/category metadata so users can identify records without relying on truncated labels.
- Asset Detail offers one browser URL-backed tab row for `ภาพรวม`, `การถือครอง`, `การดำเนินการ`, and `ตรวจนับ`. A tab shows only its relevant information group; it does not alter asset data, custody/responsibility data, permissions, or lifecycle behavior. Before checkout, check-in, or transfer saves, the same existing payload is shown in a focused review dialog so the operator can verify the asset, people/location, resulting status, and evidence before confirming.
- To install or remove a component, open `การถือครอง` then `จัดการส่วนควบ`. The Component Manager keeps the parent Asset Tag visible, accepts a scanned asset label or a search term of at least two characters, then uses identify, details, and review steps before calling the existing component endpoint. Installation can include role, slot, date, reason, and image evidence. Removal uses one in-app dialog with optional reason/evidence. It returns to the same asset's custody tab; view-only users can inspect relationships/history but cannot mutate components. Component candidate lookup stays empty below two characters and long history is revealed in small batches.

## Audit Counting

- Create audit rounds from filtered asset candidates.
- Preview candidates before creating a round.
- When an in-scope parent asset has installed components, audit preview/create includes those active installed component assets automatically. Components remain separate audit items, but the scan page shows them under the parent so field auditors can scan the component QR, confirm it with the parent when the label is inaccessible, or record the component as missing directly from an in-app dialog. Confirming a component with the parent saves immediately and does not require an extra note; if the auditor has not entered a remark, the system records the standard confirmed-with-parent reason in the audit trail. Recording a missing component also avoids browser prompts, does not require a remark, optionally accepts an evidence photo, and attaches that photo to the reviewable `audit_finding` record.
- Parent asset operations that move or reassign the physical package sync supported fields to installed components with movement logs. Audit correction sync is limited to fields the audit workflow updates today: location/custodian from scan correction and location/custodian/department from approved findings. Branch does not change through audit scan, and condition/status remain per asset.
- The audit rounds list is action-first for large counting workloads: the page summarizes next actions for continuing scans, reviewing mismatches, and closing ready rounds, and exposes quick filters for all/open/pending/review/mismatch/ready-to-close rounds. Switching quick filters updates the URL-backed view without jumping the dashboard scroll position back to the top.
- Audit rounds can be cancelled (`ยกเลิก`) with a required reason when a test or incorrect-scope round should stop. Cancellation preserves historical audit items, scan history, findings, evidence, and already-applied master changes; it does not rollback prior work. A cancelled round is read-only, excluded from operational queues and current coverage, and remains accessible only for history/review with cancellation time, user, and reason metadata.
- Opening audit round detail, pending list, or scan from `/audit/rounds` preserves the current rounds view/search through a sanitized `returnTo` value. The scan and pending pages return to the round detail, and the round detail returns to the originating rounds list context.
- Scan printed Asset Label QR codes or manually enter asset identifiers. The Audit Scan camera uses the same native-resolution Asset QR decoder as `/asset-management/scan`, defaults to the rear-camera scan-first flow, and stops after a successful QR read so the selected target is locked before the auditor saves or changes the result. Use `เปลี่ยน / สแกนใหม่` to clear the locked target and return to the scan input. QR scans stay exact/raw-value based for traceability; partial matching is manual-only.
- The mobile scan workspace keeps one camera preview and its torch/zoom/stop controls inside or immediately beside that preview. Its sticky progress strip is followed by the scan/manual input, the locked target and system data, then the in-round decision. Evidence, component context, recent scans, pending work, and offline retry are supporting context. The fixed action bar appears only after an in-round target exists and reserves its height plus the browser safe area; it offers matched, mismatch/Finding, and change-target actions. Marking an expected asset not found remains a Pending List action.
- Controller browser QA observed one primary and one supporting region without body overflow at 375/390/414/768/1280/1440, including the same-node two-column form grid at 1280. The 375px Recent Scans header measured 284px by 86px and remained readable after its mobile stacking fix. With an in-round target, exactly one mobile action region appeared below 768px with matched, mismatch/Finding, and change/rescan actions only; the hook is hidden at 768px and above. Controller camera permission did not yield a video stream, so camera, torch, and zoom remain real-device checks.
- The scan page uses the sticky progress header as the single source for round progress. Before an asset is selected, it shows the full compact progress bar with checked/pending counts plus a photo-queue badge; after an asset or out-of-scope record is selected, it collapses into a shorter mobile-friendly strip showing checked/total, pending count, photo queue, and the pending-queue action. It intentionally does not repeat the same numbers as separate metric cards.
- The sticky progress header also has a `คิวรอตรวจ {count}` quick action. It opens a collapsible inline preview of pending `audit_items` for the current round so auditors can quickly load expected data, with expected location, custodian, and department shown directly on each card. The full pending list is mobile-first: auditors can search by Asset Tag/name/location/custodian, scan a specific pending item, open the asset detail, or mark it not found from full-width card actions. The mark-not-found action opens an in-app mobile dialog instead of a browser prompt; remark and evidence photo are optional, and uploaded evidence is attached to the created/pending audit finding under `audit_finding`. When auditors open the full pending list from the scan page, its back action returns to the same audit scan workflow.
- Before a walking audit starts, the scanner can remember a browser-local work area by expected Location and Department for the current `auditRoundId`. This filters only the pending queue, initial manual picker, and progress display; direct QR/manual lookup still resolves against the complete existing round, and the stored work area does not create a new audit round, audit item, finding, or API payload.
- Before an item is selected, the scan/manual entry panel is visually emphasized as the main starting point. This keeps field auditors focused on scanning or entering an Asset Tag instead of scrolling through the full item selector.
- If the camera cannot read the label or the auditor starts typing manually, the scan input shows bounded suggestion cards after at least two characters. Suggestions come from the current round by Asset Tag, name, expected location, custodian, or department; one clear match can be selected quickly, while multiple matches require the auditor to choose a row. The larger collapsible picker remains available for broader queue search and still lists pending items first when no search text is entered.
- Normal camera-ready status and camera selector controls are hidden to keep the walking-scan panel short. Camera utility UI appears only while the scanner is active, after a QR reference is decoded, or when the browser reports a camera issue.
- When supported by the browser and rear camera track, the scanner preview shows a flashlight toggle for low-light locations such as under desks and 1x/2x/3x zoom buttons for small QR labels. The 1x button returns the live preview to the unzoomed camera view. Each control is hidden on unsupported devices, and the flashlight is turned off when scanning stops or the camera changes.
- After an asset is selected, the scan page shows the system data that auditors need for field comparison: current location, custodian, department, and condition.
- The scan result card is focused on the current QR/manual outcome. A separate compact `สแกนล่าสุด` panel opens and collapses the full recent-read list, including matched, mismatch, out-of-scope, unknown asset, found-later, saved, and offline-queued results. Its header shows `แสดง {count} รายการล่าสุด` instead of a count/retention-limit fraction, so auditors do not confuse recent-read history with total checked progress. When collapsed, no recent rows or edit actions remain visible, keeping the mobile scan screen short. Use this as a field sanity check; the official audit state still comes from the saved audit items, scan history, findings, and offline queue sync.
- Fast field behavior remains the default Audit Scan behavior, so the normal field UI does not expose extra mode switches. Auditors choose `บันทึกพบตรง` to save a matched result immediately, or `บันทึกข้อมูลไม่ตรง / Finding` to open the detailed actual-value fields and inline evidence area. On mobile, these post-scan actions move into a fixed bottom action bar where `บันทึกพบตรง` spans the first row as the primary action, while `บันทึกข้อมูลไม่ตรง / Finding` and `เปลี่ยน / สแกนใหม่` stay below it. The old evidence-scroll shortcut is intentionally removed; mismatch evidence is collected inside the revealed mismatch flow with a visible required-photo counter.
- Audit photo evidence is free-form and optional for matched results. Auditors can add multiple photos and optionally tag them as general evidence, checklist labels, Serial Number, or Asset Tag for easier review. Queued photos show a fixed-size mobile thumbnail, filename/tag, and whether the photo will attach to the current Finding or audit result before saving.
- Record found, mismatch, not-found, out-of-scope, and correction cases. Scan results distinguish between an expected asset found in the round, an asset that exists in the system but is outside the round (`Out-of-scope`), and an unknown QR/asset code that is not registered in the system. Marking an item as not found is done from the pending list/zone queue for expected assets that could not be located, not from the successful scan action area.
- When a scanned value is not in the current round, the client calls `POST /api/audit-rounds/{id}/scan-lookup` instead of Global Search. The lookup requires `audit:edit`, resolves printed `/q/a/{assetId}` labels and legacy identifiers by `Asset.id`, `assetTag`, `serialNumber`, or `fixedAssetCode`, and returns `Out-of-scope` only when the asset exists in the system but has no `audit_item` in the round. Unknown asset messaging is reserved for values that do not resolve to an active asset. The lookup does not write audit records; the existing scan save action still records the out-of-scope finding and scan history after user confirmation.
- Audit Scan clears transient scan feedback whenever the auditor selects or clears a target, so out-of-scope feedback cannot carry into the next in-round decision.
- Out-of-scope scan save can record actual field values found on site. If those values differ from the current master asset record, the save requires evidence and links that evidence to the reviewable field-specific findings; approving those findings is what updates the master asset and movement log.
- If an item was previously marked not found and is later scanned in the same audit round, the existing found-later recovery path should resolve the not-found finding through the current audit scan logic.
- Review findings and close rounds with segregation-of-duties protection.
- Use `/audit/findings` as the existing `รายการไม่ตรง` follow-up workflow after field scanning. The page groups existing audit findings into pending review, open action, overdue, and closed queues, shows system-vs-found values together, keeps review/action/closure controls in place, and exports Excel/PDF with the same active filters. Normal freshness feedback is a compact loaded-at metadata strip with refresh; a larger stale warning appears only after the page has been open past the configured stale window. Reviewers approve/reject through a decision modal, can open `audit_finding` evidence attachments inline from the finding card/table, and approving a correctable finding checks whether the master asset changed after the finding was reported before applying corrections.
- Disposal follow-up links opened from `/audit/findings` carry the current findings queue/search as a sanitized return context so reviewers can return to the same resolution queue after the disposal workflow.

## Maintenance And PM

- Create repair tickets from the maintenance page or asset quick actions.
- Opening a repair ticket sets the asset to `Pending Repair` when that active status exists. The create-ticket form/API should not require `returnDate`; `returnDate` is collected when the repair is closed.
- Opening maintenance ticket detail or print from a filtered maintenance list preserves the current tab, status, search, and asset filter through a sanitized `returnTo` value. Ticket detail exposes a Back action to that originating list context.
- Track status, SLA, evidence, costs, assignee/vendor, close checklist, and inspector.
- Close maintenance by choosing whether the asset returns to `Ready` or should move to `Pending Disposal`. Do not use the generic asset edit form for protected repair/disposal status changes.
- Maintenance tickets offer a URL-backed `ตาราง` or `บอร์ด` layout. The switch preserves the existing search/filter context and changes presentation only; the same ticket status, SLA, detail, and PM flows remain authoritative.
- Create Preventive Maintenance plans.
- Scheduled PM generation uses the scheduler heartbeat and web-configured schedule.
- PM history is visible from related asset detail flows.

## Disposal

- `/disposal` is the queue and decision workspace; request creation lives at `/disposal/new`, while shared 2-100 item packets use `/disposal/batch/new`. Both create flows search eligible assets from the server after two characters and return at most 50 results instead of loading the full Asset Register into the page. Maintenance, audit findings, and asset quick actions preserve their source and sanitized return context when opening the create route.
- Opening disposal request detail or print from a filtered disposal list preserves the current status/search filter through a sanitized `returnTo` value. Request detail exposes a Back action to that originating list context.
- Source assets must be active and operational; protected states such as Pending Disposal, Disposed, Retired, Lost/Missing, Under Maintenance, and Pending Repair cannot start another disposal request. The readiness guard also blocks open checkout, active maintenance, operational audit/finding work, installed parent/child component relations, and active license assignments. Search results keep blocked assets visible with the exact reasons instead of silently hiding them. Duplicate open requests are rejected before any status mutation.
- Creating a request moves the asset to Pending Disposal. The requester, optional assigned approver, and executor must be active employees; requester and assigned approver are kept distinct. Approval (`disposal:approve`) and actual execution (`disposal:edit`) remain separate, and configured segregation of duties blocks requester/creator self-approval plus approver execution.
- Disposal presentation makes the lifecycle explicit: `รออนุมัติ` -> `อนุมัติแล้ว รอดำเนินการ` -> `ดำเนินการเสร็จสิ้น`, with rejected requests shown separately. The detail workspace shows the current owner/queue, one permission-aware next action, progressive decision/execution sections, evidence, and translated movement history.
- Decision and execution forms are type-aware: rejection requires a reason; sale shows buyer/reference/actual sale fields; donation/general disposal require recipient/destination and document; destruction/lost require document and execution/incident detail. Irrelevant financial fields stay hidden. Every execution requires evidence and can end only in Disposed or Retired.
- Batch disposal stores one auditable packet and creates 2-100 normal child disposal requests in one transaction. Every child remains authoritative for independent approval, execution, item evidence, asset movement, and SOD. Conditional asset-status updates prevent concurrent create requests from claiming the same asset, while document-number creation retries bounded Prisma unique conflicts. Shared evidence selected during creation is uploaded once to the batch workspace and inherited read-only by every child; upload failures do not roll back successfully created requests and can be retried from the batch workspace. `/disposal/batches` provides batch history and progress, while `/disposal/batches/{id}` is the batch result/workspace.
- The disposal queue preserves filters/pagination in return navigation, shows exact result ranges, validates date order, keeps batch source links, uses a sticky desktop action column, and progressively reveals secondary mobile-card details. On mobile request detail, the stage-aware review/execute control owns the primary bottom action; asset, print, and history remain secondary actions without duplicating the desktop toolbar.
- Apply `prisma/manual-migrations/2026-07-13-add-disposal-batches.sql` only after an approved backup/change window. Roll back by restoring the pre-change database backup; do not drop packet/child relations after production records exist.

## Reports And Export

- Asset register export and report exports use current filters where supported.
- Cross-scope filters are supported by Dashboard drilldowns, Asset Register export, Reports, and Asset Overview export. Asset register export includes owner/custodian/home/current location branch context and cross-scope flags; Asset Overview export includes cross-scope summary metrics and a Cross Scope sheet for review.
- Audit, disposal, maintenance, and asset overview exports support operational review.
- Operators can save named asset-report filter presets in the current browser. Presets are intentionally device-local, are not shared with other users or synced between browsers, and only reopen the existing reports page with its URL filters.
- PDF output uses bundled Thai fonts unless production overrides are configured.

## Admin Operations

- Brand / Model master data uses a compact brand navigator beside the model workspace. Users choose a brand on the left, then search/filter/edit models on the right without scrolling through a separate brand table; the navigator is intentionally narrow on desktop so the model table keeps more usable width and avoids unnecessary horizontal scrolling. The visible brand counts use active model and active asset group counts, matching the model table and avoiding soft-deleted model mismatches. Count links support drilldown: model counts select/open the matching model workspace, brand asset counts open Asset Register filtered by brand, and model asset counts open Asset Register filtered by model.
- Supplier master data treats the supplier code field as `Tax ID / Supplier Code`; Thai labels emphasize `เลขประจำตัวผู้เสียภาษี / รหัสผู้ขาย` while still allowing legacy supplier codes.
- Company, Branch, Location, Employee, and Supplier lists share a compact workspace navigation strip. Existing per-page filters, active-only list policy, pagination, drilldowns, row actions, validation, and delete guards remain unchanged.
- Category master data uses soft delete with unique codes: recreating a deleted category reactivates the inactive row, referenced categories cannot be deleted or deactivated, and custom-field templates can be edited while the category remains active. New category creation uses a create-only custom-field template payload, while updates/reactivations use template replacement, so categories can be added even when the template list is empty. Category model and asset counts drill into the filtered Brand/Model workspace or Asset Register instead of being passive numbers.
- Manage users, roles, permissions, workflow policy, notifications, LDAP settings, scheduler settings, readiness checks, storage governance, and system logs.
- Asset Tag Prefix settings are managed as prefix groups: enter one Prefix, search/select available categories on the left, move selected categories to the right, then save. The UI writes the existing `categoryId -> prefix` setting format, filters stale/inactive category ids out of visible group counts, and categories still use the configured prefix during generated asset tags while falling back to category code when unassigned.
- Depreciation policy is configured in Settings as a policy builder: admins set straight-line defaults, assign active categories to policy groups, preview monthly depreciation and net book value, and can inspect the generated JSON in advanced mode. Reports still use purchase date as the depreciation start date.
- System Settings includes a key/description/section finder and changed-setting links that return the operator to the relevant existing settings tab. The unsaved-change review and save semantics remain unchanged. Empty, error, and access-denied surfaces reuse `ActionEmptyState` so state messaging and action spacing stay consistent across the dashboard.
- Storage Governance dry-run actions show an action column for archive/review decisions. Only orphan file actions expose the archive button, and translation coverage tests guard every storage page message key in Thai and English.
- System logs present readable record labels and before/after summaries where available.
